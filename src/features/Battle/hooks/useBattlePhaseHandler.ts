export const useBattlePhaseHandler = () => {
	const handleAttackPhase = (success: boolean) => {};

	const handleDefendPhase = (success: boolean) => {};

	return {
		handleAttackPhase,
		handleDefendPhase,
	};
};
